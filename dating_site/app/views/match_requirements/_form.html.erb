<%= form_for(@match_requirement) do |f| %>
  <% if @match_requirement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match_requirement.errors.count, "error") %> prohibited this match_requirement from being saved:</h2>

      <ul>
      <% @match_requirement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requirement_type %><br>
    <%= f.text_field :requirement_type %>
  </div>
  <div class="field">
    <%= f.label :requirement %><br>
    <%= f.text_field :requirement %>
  </div>
  <div class="field">
    <%= f.label :importance %><br>
    <%= f.number_field :importance %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
