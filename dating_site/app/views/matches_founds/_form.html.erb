<%= form_for(@matches_found) do |f| %>
  <% if @matches_found.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@matches_found.errors.count, "error") %> prohibited this matches_found from being saved:</h2>

      <ul>
      <% @matches_found.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
